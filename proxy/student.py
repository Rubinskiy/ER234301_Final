class Student:
    def __init__(self, name, is_verified=False):
        self.name = name
        self.is_verified = is_verified

# Note
# Class to create the student object, only name and is_verified are needed